# Archived memories

Historical memory fragments kept for audit. The MemoryChain loader skips
anything under `archive/` — these are stored for completeness but not
served to the LLM as cross-game context.

To re-activate, copy a file out of `archive/` into the parent `seed/`
directory and rename to `NNN_<slug>.md`.
